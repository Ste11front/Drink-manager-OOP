class Cellar { //library
    constructor (beverage = []) {
        this.beverage = beverage;
    }
    addBeverage(bev) {
        this.beverage.push(bev);
    }
    deleteBeverage(bev) {
        this.beverage = this.beverage.filter((_, i) => i !== index);
    }
    










}
